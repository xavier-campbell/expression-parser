This is a very basic expression parser, at the moment it does not do user input or negative numbers, those are to be added.

<h3>Usage</h3>
Currently to use the expression parser edit the list of expressions present in the string in main.c, it will evaluate all of those.
